(:background, :glance, :noLowMemory)
const debug = true;

(:background, :glance, :lowMemory)
const debug = false;

(:background, :glance, :noLowMemory)
const minDebug = true;

(:background, :glance, :lowMemory)
const minDebug = false;
